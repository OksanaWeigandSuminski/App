<%= form_with(model: security_and_privacy_vulnerability, local: true) do |form| %>
  <% if security_and_privacy_vulnerability.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(security_and_privacy_vulnerability.errors.count, "error") %> prohibited this security_and_privacy_vulnerability from being saved:</h2>

      <ul>
      <% security_and_privacy_vulnerability.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label :product_id, :class => "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
    <%= form.number_field :product_id, :class => "form-control"%>
  </div>
  </div>

  <div class="form-group row">
    <%= form.label :cve_id, :class => "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
    <%= form.text_field :cve_id, :class => "form-control" %>
  </div>
  </div>

  <div class="form-group row">
    <%= form.label :severity, :class => "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
    <%= form.text_field :severity, :class => "form-control" %>
  </div>
  </div>

  <div class="form-group row">
    <%= form.label :description, :class => "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
    <%= form.text_area :description, :class => "form-control" %>
  </div>
  </div>

  <div class="form-group row">
    <%= form.label :advisories, :class => "col-sm-2 col-form-label" %>
    <div class="col-sm-10">
    <%= form.text_area :advisories, :class => "form-control" %>
  </div>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
